package com.ssm.view;

import android.view.View;
import android.view.View.OnClickListener;

import com.example.cyclists.R;
import com.ssm.controller.HomeFragment;
import com.ssm.controller.MainActivity;
import com.ssm.controller.SettingsFragment;

public class SettingsLayout {

	SettingsFragment fragment;
	
	public SettingsLayout(SettingsFragment instance) {
		fragment = instance;
	}
	
	public void init(){
		fragment.getView().findViewById(R.id.menu_button_settings).setOnClickListener(buildButtonClickListner());
	}
	
	private OnClickListener buildButtonClickListner(){
		return new OnClickListener(){
				
					@Override
					public void onClick(View v) {
						switch(v.getId())
						{
						case R.id.menu_button_settings:
							((MainActivity)fragment.getActivity()).getLayout().open_button(v);
							break;
						}
					}
				};
	}
	
}
